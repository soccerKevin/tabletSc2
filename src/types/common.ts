export interface Position {
  lat: number;
  lng: number;
}

export interface SelectionBox {
  left: number;
  top: number;
  width: number;
  height: number;
}

export interface TouchPoint {
  x: number;
  y: number;
}